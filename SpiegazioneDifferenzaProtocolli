Dagli screen si notano le differenze tra i due tipi di protocolli http e https. Si nota che nel protocollo HTTPS i pacchetti vengono cifrati e quindi non è possibile leggere il contenuto, questo perchè nel protocollo https entra in azione anche il protocollo TLS che crea un tunnel criptato di conseguenza i dati che viaggiano all'interno sono criptati. Nel protocollo HTTP invece, i dati non vengono criptati perchè non entra in gioco il protocollo TLS.
